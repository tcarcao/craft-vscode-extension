# Everything except what we explicitly include
**/*

# Include only bundled distribution files
!dist/**

# Include resources (WASM, queries, icons)
!resources/**

# Include essential config files
!package.json
!language-configuration.json
!LICENSE
!readme.md

# Include external dependencies needed by bundled server
!node_modules/web-tree-sitter/**

# Exclude source maps from dist
dist/**/*.map

# Exclude test files and development artifacts
**/.vscode-test/**
**/test/**
**/.eslintrc
**/.prettierrc